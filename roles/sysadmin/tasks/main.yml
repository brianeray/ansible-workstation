---
# General system administration goodies. Some of these are already present
# and/or configured on some of my distros, but some aren't.

- name: Install apt-packaged diagnostics
  ansible.builtin.apt:
    name:
      - apt-listchanges
      - nethogs
      - sysstat
    state: present
